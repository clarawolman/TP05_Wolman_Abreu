<!---@{
    ViewData["Title"] = "Sala 2 - Cáliz";
}
<h2>Sala 2 - El Cáliz de Fuego</h2>
<p>Arrastrá tu nombre al cáliz.</p>

<div id="area">
    <div id="nombre" draggable="true" ondragstart="drag(event)" style="padding:10px; background:lightblue;">Tu Nombre</div>
    <img id="caliz" src="/img/caliz.png" style="width:100px;" ondrop="drop(event)" ondragover="allowDrop(event)" />
</div>

<form method="post" id="formResultado" style="display:none;">
    <button name="respuesta" value="correcta">Nombre aceptado por el cáliz</button>
</form>

<script>
function allowDrop(ev) {
    ev.preventDefault();
}
function drag(ev) {
    ev.dataTransfer.setData("text", ev.target.id);
}
function drop(ev) {
    ev.preventDefault();
    var data = ev.dataTransfer.getData("text");
    if (data === "nombre") {
        document.getElementById("formResultado").style.display = "block";
    }
}
</script>!--->

@{
    ViewData["Title"] = "Sala 2 - Cáliz";
}
<h2>Sala 2 - El Cáliz de Fuego</h2>
<p>Hacé clic en tu nombre para tirarlo al cáliz e iniciar la prueba.</p>

<form method="post" asp-action="TirarNombreAlCaliz">
    <button type="submit" style="padding:10px; background-color:lightblue;">Tu Nombre</button>
</form>

SALA 2 POCIONES
@{
    ViewData["Title"] = "Sala 2 - Pociones";
}
<h2>El Desafío de las Pociones</h2>
<p>Una de estas pociones te permitirá avanzar. Las otras son peligrosas.</p>
<img src="/img/mesa_pociones.png" style="width:100%; max-width:600px;" />

@if (ViewBag.Mensaje != null)
{
    <p style="color:red;">@ViewBag.Mensaje</p>
}

<form method="post" asp-action="ResolverPociones">
    <button name="pocion" value="correcta">🧪 Poción del medio</button>
    <button name="pocion" value="roja">🧪 Poción roja</button>
    <button name="pocion" value="negra">🧪 Poción negra</button>
    <button name="pocion" value="verde">🧪 Poción verde</button>
</form>

SALA 2 FINAL

@{
    ViewData["Title"] = "Sala 2 - Cáliz Final";
}
<h2>¡Felicidades!</h2>
<p>El cáliz ha aceptado tu victoria.</p>
<img src="/img/caliz_brilla.png" style="width:150px;" />

<form method="post" asp-action="Sala2">
    <button name="respuesta" value="correcta">Avanzar a la siguiente sala</button>
</form>
